# dépendances
/node_modules

# build Next.js
/.next
/out
next-env.d.ts
*.tsbuildinfo

# environnement (ne jamais versionner les secrets)
.env
.env*.local

# messages reçus via le formulaire de contact (stockage local)
/data/messages.json

# divers
.DS_Store
npm-debug.log*
.vercel
